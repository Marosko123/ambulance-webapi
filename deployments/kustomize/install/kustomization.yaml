apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml

configMapGenerator:
  - name: mb-ambulance-webapi-mongodb-init
    files:
      - params/init-db.js
  - name: mb-ambulance-webapi-config
    literals:
      - database=mb-ambulance
      - collection=ambulance
patches:
- path: patches/webapi.deployment.yaml
  target:
    group: apps
    version: v1
    kind: Deployment
    name: mb-ambulance-webapi
