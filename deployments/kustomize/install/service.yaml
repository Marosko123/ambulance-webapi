kind: Service
apiVersion: v1
metadata:
  name: mb-ambulance-webapi
spec:
  selector:
    pod: mb-ambulance-webapi-label
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: webapi-port
